---
paths:
  - "android/**"
  - "app.json"
  - "app.config.*"
  - "plugins/**"
  - "eas.json"
---

# Native Android rules (Expo, Android-first)

## Stay in the managed workflow
- `android/` is generated by `expo prebuild` and may be regenerated at any time. Do not hand-edit
  files under `android/`; express native changes as Expo config (`app.json` / `app.config.*`) or
  a config plugin in `plugins/`. If a change truly needs a hand edit, stop and ask the owner.
- Do not add or upgrade a native module (anything that needs prebuild or a new Gradle dependency)
  outside an OpenSpec change that names it in `design.md`.
- Nothing in shared code may assume Android: platform-specific code goes behind one adapter
  module with `Platform.select`/`.android.ts` files, so an iOS build stays possible.

## Permissions and background work
- Every Android permission is declared in the Expo config with a one-line comment on why, and the
  OpenSpec change that adds it has a requirement explaining the user-facing reason.
- Notification access (reading other banks' pushes) is the only background capability in scope.
  Parsing happens on the device; nothing read from notifications is sent anywhere.
- No analytics, crash reporting or remote config SDKs without an explicit spec change.

## Secrets and release
- Never commit keystores (`*.jks`, `*.keystore`), `google-services.json`, `local.properties`,
  `.env*`. They are denied to the agent; if a task seems to need one, stop and ask.
- The agent does not run `eas build`, `eas submit` or `eas update`; the owner does.

## Verification
- `npm run verify` does not cover native code. When a change touches these paths, say so
  explicitly in the task and change summary, and point at the CI `android` job (Gradle
  `assembleDebug`) as the check. Do not claim a native change works from typecheck alone.
- Run `npx expo-doctor` after changing the Expo config or dependencies.
