# BACKLOG — черга автономної роботи

Черга для скіла `auto-work` (`.claude/skills/auto-work/SKILL.md`). Агент бере верхній
невідмічений пункт і проводить його повним циклом з CLAUDE.md (propose → spec-reviewer →
apply → verify → diff-reviewer → commit → archive), відмічає `[x]` і запускає наступну
сесію. Запуск — вручну (`/auto-work` у будь-якій сесії) або сам собою (scheduled task
`auto-work`, працює поки відкритий застосунок).

Власник редагує цей файл вільно: додає, переставляє, уточнює. Порядок = пріоритет.
Пункт з позначкою `[?]` заблоковано питанням у розділі «Питання» — відповідь пишеться
просто під питанням, наступний запуск її підхопить і зніме позначку.

Джерело задумів — `docs/tech-task.md` §5; терміни — строго з `docs/glossary.md`.
`git push` агент не робить ніколи; публікація — рішення власника.

## Черга

- [x] Дозавершити зміну `monthly-picture` (19/19): зроблено паралельною сесією — коміти
      22cd09c (реалізація) і 2d61446 (архів).
- [x] Зміна `categories-rules` (30/30): зроблено. Коміти 9a70db7 (реалізація) і ac1fd3e
      (архів). Зарезервовані id тепер указують на реально засіяні рядки — і не завдяки seed, а
      завдяки самій міграції 0003: без цього перетворення `category_id` на FK валить застосунок
      на пристрої, де вже є витрата (перевірено на емуляторі). Коригування-мінус як «витрачено»
      категорії «Коригування» — `seed.test.ts`. 406 тестів, смоук на 22 рядки.
- [x] `saldo-import` — крок 6 tech-task, частина 1 (движок): зроблено (22/22). Коміти
      a4e6a14 (реалізація) і b7c9294 (архів). Чистий TypeScript у `src/saldo/`: `parse.ts`
      (CSV), `interpret.ts` (подвійний запис → транзакції), `survey.ts` (мапа рахунків, борги),
      `verify.ts` (звірка балансів), плюс `scripts/saldo-dry-run.ts`. Запису в БД і UI немає —
      це частина 2. 491 тест.
- [x] `saldo-import-screen` — крок 6, частина 2 (екран): зроблено (27/27). Коміт e0e6489.
      Секція «Імпорт Saldo» в Налаштуваннях веде потік: файл → мапа рахунків → люди для «Борг» →
      звірка → атомарний коміт (`src/db/import-repo.ts`, одна транзакція БД, маркер імпорту в
      міграції 0004). Дельта `transactions` для знакового доходу вже приїхала з частиною 1, тож
      тут її не було. «Відсотки» стало **зарезервованим** джерелом (як «Комісія») — інакше
      пропозиція FR-T9 могла б послатися на перейменований чи заархівований рядок; створений
      вручну рядок seed «усиновлює», а не дублює. Комісія і відсотки зроблені взаємовиключними:
      з рахунку-боргу комісія не пропонується, відсотки — лише коли обидві ноги рівні. 555 тестів,
      смоук на емуляторі з реальним експортом (27 рахунків, звірка збіглася з сухим прогоном) і
      коміт зрізу на 188 транзакцій.
- [ ] `monobank-sync` — крок 7: токен, транзакції без дублів, баланси, «звірити», курс
      (FR-S2, FR-A3, FR-T6).
- [ ] `bank-notifications` — крок 8: Android notification listener, парсери, чернетки
      транзакцій (FR-S3).
- [ ] `limits-goals-reports` — крок 9: ліміти, цілі, обидва графіки (FR-L1–L2, FR-R1–R3).
- [ ] `investments-value` — крок 10: поточна вартість, прибуток/збиток (FR-I1–I2).
- [ ] `backup` — крок 11: експорт/імпорт файлу бекапу (FR-B1–B2).

## Питання

(порожньо — агент пише сюди, коли не може спитати інтерактивно; власник відповідає
просто під питанням і прибирає `[?]` не треба — агент зробить це сам)
