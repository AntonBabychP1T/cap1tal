# BACKLOG — черга автономної роботи

Черга для скіла `auto-work` (`.claude/skills/auto-work/SKILL.md`). Агент бере згори до
трьох пунктів, які не конфліктують між собою, і веде кожен у власній гілці-worktree
повним циклом з CLAUDE.md (propose → spec-reviewer → apply → verify → diff-reviewer).
Готові гілки вливаються по одній (`merge --squash` → `verify` → commit), далі зміни
перевіряються на емуляторі (`smoke-runner`), правляться за побаченим і аж тоді
архівуються; пункт відмічається `[x]` з рядком результату. Запуск — вручну (`/auto-work`
у будь-якій сесії) або сам собою (scheduled task `auto-work`, працює поки відкритий
застосунок).

Порядок у списку — це пріоритет, але сусідні пункти можуть піти в одну «хвилю»
паралельно. Не паралеляться (і це навмисно): міграції БД, зміни `package.json`,
нативні зміни (`app.json`, `plugins/`, `modules/`) і два пункти в одному capability.

Власник редагує цей файл вільно: додає, переставляє, уточнює. Порядок = пріоритет.
Пункт з позначкою `[?]` заблоковано питанням у розділі «Питання» — відповідь пишеться
просто під питанням, наступний запуск її підхопить і зніме позначку.

Джерело задумів — `docs/tech-task.md` §5; терміни — строго з `docs/glossary.md`.
`git push` агент не робить ніколи; публікація — рішення власника.

## Уже зроблено

- [x] Кроки 0–5: каркас застосунку, домен і БД, рахунки та ручні транзакції, місячна
      картина, категорії, джерела й правила. Працюють локальні міграції, розрахункові баланси,
      CRUD рахунків і транзакцій, категоризація та основні екрани.
- [x] `saldo-import` і `saldo-import-screen` — крок 6: CSV Saldo розбирається, рахунки
      мапляться, результат звіряється і записується атомарно. Імпорт перевірено на реальному
      експорті: 27 рахунків і 188 транзакцій.
- [x] `monobank-sync` — крок 7: токен зберігається захищено на пристрої, рахунки
      зв'язуються з monobank, транзакції підтягуються без дублів, показується баланс банку і
      працює «Звірити». Окрема зміна `monobank-connect-flow` уже виконана на 9/10: лишився
      фінальний diff-review.
- [x] `bank-notifications` — **движок** кроку 8: є модель прочитаного сповіщення,
      fingerprint-дедуплікація, загальний та підключні банківські парсери, чернетки,
      підтвердження/відхилення й автопідтвердження за правилом. Усі 14/14 задач цієї частини
      виконані. Це ще не означає, що Android фізично передає сповіщення застосунку.
- [x] `limits-goals-reports` — крок 9: ліміти, цілі, графіки та екрани. Заархівовано
      2026-08-31, вісім специфікацій злиті у `openspec/specs/`; смоук на емуляторі пройшов
      без дефектів.

## Ще треба зробити — у порядку пріоритету

- [ ] Закрити майже готові зміни: `monobank-connect-flow` (9/10), `saldo-import-merge` (4/5),
      `saldo-import-simple-debts` (11/13), після цього архівувати `first-run-onboarding`
      (10/11; його остання задача чекала архівації `limits-goals-reports` — тепер вона знята).
      `limits-goals-reports` заархівовано 2026-08-31: diff-review знайшов смужку в розбивці
      Місяця, яку код малював, а жодна вимога не описувала — її внесено у специфікацію, а не
      прибрано; смоук на емуляторі пройшов без дефектів.
- [ ] `bank-notifications-screen` — завершити крок 8. Перша половина вже зроблена зміною
      `bank-notifications-capture` (заархівована 2026-08-31): нативний
      `NotificationListenerService` живе у власному локальному Expo-модулі
      `modules/notification-capture/` (config plugin не знадобився — модуль несе свій
      manifest), доступ повертає реальний стан замість `unsupported`, а `packageName`, час,
      title і text ідуть у чернетковий движок через platform-порт; системні, власні та
      невідстежувані пакети відкидаються, `com.ftband.mono` заборонений двічі. Захоплене
      чекає у обмеженій черзі на пристрої й доставляється, доки його не підтвердять.
      Перевірено на емуляторі: дозвіл в обидва боки, захоплення, фільтр, повторна доставка,
      перезавантаження телефона і місток JS↔native. **Лишилося:**
      - додати таблиці й репозиторії для відстежуваних застосунків, fingerprint-ів і чернеток;
        записувати fingerprint + чернетку/транзакцію атомарно, щоб перезапуск service не створював
        дублів;
      - у Налаштуваннях пояснити дозвіл, відкрити системний екран «Доступ до сповіщень», показати
        реальний стан, дати вибрати банківський застосунок і прив'язати його до рахунку (monobank
        не пропонувати, бо він синхронізується через API);
      - на Головному показати чернетки, дати підтвердити, виправити або відхилити; незрозумілий
        формат не губити, а залишати сирою чернеткою;
      - перевірити на фізичному Android/development build реальними тестовими сповіщеннями,
        перезапуском застосунку й service, повторною доставкою та відкликанням дозволу. Expo Go
        цей service не містить і для перевірки не підходить.
      - **провести кожне додавання відстежуваного застосунку через порт**: `addWatch` у
        `src/domain/notifications/draft.ts` досі пропускає `com.ftband.mono*`, бо заборона за
        рішенням D6 живе в `src/platform/notification-capture.ts`. Поки жоден код не кличе ні
        того, ні того, але екран мусить ходити через порт, інакше picker і пристрій
        розійдуться в правилах.
      - дрібниця з емулятора: у debug-збірці банер LogBox «Open debugger to view warnings.»
        перекриває кнопку «Оновити стан» унизу «Перших кроків» і з'їдає перший тап. Лише
        debug, але дратує при смоуках.
- [ ] Гарантувати сумісність JS і нативної development build після додавання Expo-модулів.
      На ручному запуску iOS 2026-09-01 встановлена збірка падала до першого екрана з
      `Cannot find native module 'ExpoPushTokenManager'`; повна перебудова через
      `expo run:ios` виправила запуск. Треба додати відтворювану перевірку, яка після зміни
      нативних залежностей перебудовує dev client, запускає його на чистому симуляторі й
      ловить відсутні модулі до смоуку UX; у документації явно сказати, коли hot reload уже
      недостатній і потрібна нова нативна збірка.
- [ ] Щоденна придатність — те, чого забракло під час ручного проходу застосунком на
      емуляторі 2026-09-01. Жодна вимога цього не покриває: специфікації описують, що
      застосунок уміє, а не як ним користуються щодня. Розбити на окремі зміни за
      capability, порядок усередині — це теж пріоритет:
      - **сума грошей не показана ніде.** «Рахунки» дають Kartka 7050,00 UAH і
        Podushka 6000,00 UAH — і жодного підсумку; скільки всього грошей, не каже жоден
        екран. Головний теж відкривається формою запису, а не грошима, а стрічка ховається
        за два свайпи. Треба підсумок на «Рахунках» (з приблизною гривнею для валютних, як
        це вже робить місячна картина) і рядок з грошима вгорі Головного;
      - **транзакцію неможливо знайти.** Стрічка Головного — останні 50 (`FEED_SIZE`
        у `src/app/(tabs)/index.tsx`), гортати далі нема як, і пошуку немає ніде в
        застосунку. Після імпорту Saldo (188 транзакцій) історія фактично недосяжна.
        Треба пошук за сумою, описом і категорією та повний список з фільтром за рахунком
        і періодом;
      - **тап на рахунок відкриває перейменування, а не історію.** Найприродніший жест дає
        форму «Назва / Вид / Валюта / Зберегти / До архіву»; транзакцій рахунку не видно
        ніде. Треба: тап → рухи рахунку, а редагування — окремою дією;
      - **ручній транзакції нема куди написати опис.** `src/ui/entry-form.ts` носить
        `description` і сам зазначає «the form never asks for one»: імпорт опис кладе,
        власник — ні. «Other expense · 1200,00 UAH» через місяць нічого не означає. Треба
        поле опису у формі запису;
      - **форма запису довга й без пам'яті.** Рахунок доводиться обирати щоразу (дефолту
        немає), категорії — суцільний список чипів за алфавітом, а після «Записати» немає
        жодного підтвердження: сума обнуляється, нова транзакція лягає нижче краю екрана.
        Треба рядок останніх/частих категорій, запам'ятаний рахунок як дефолт і видиме
        підтвердження запису;
      - **«Звірити» доступна лише рахунку, зв'язаному з monobank.** Готівку й будь-який
        інший рахунок вирівняти нема як, хоча вимога capability `accounts` описує
        коригування на різницю й банк у ній не згадано. Треба дати ввести фактичний
        залишок вручну і записати коригування на різницю.
- [ ] UX-полірування після повного ручного проходу iOS 2026-09-01. Розбити на окремі
      зміни за capability; порядок усередині — пріоритет:
      - **скоротити шлях до запису витрати:** на Головному показувати 4–6 нещодавніх або
        частих категорій, решту відкривати через «Усі категорії» з пошуком; кнопку
        «Записати» закріпити над таббаром, щоб вона не зникала нижче довгого списку;
      - **вибирати очевидні дефолти до валідації:** коли доступний один рахунок — обирати
        його автоматично; після першого вибору запам'ятовувати рахунок для наступного
        запису. Зараз єдиний чип виглядає як значення, але перший submit завершується
        алертом «оберіть рахунок»;
      - **не пропонувати неможливий переказ:** з одним активним рахунком вимкнути тип
        «переказ» або замінити форму поясненням і CTA «Створити другий рахунок»; не давати
        обрати той самий рахунок одночасно «Звідки» і «Куди»;
      - **зробити залежні дії чесно недоступними:** «Оновити з monobank» без токена та
        «Синхронізувати» без приєднаного рахунку зараз приймають тап без видимого результату.
        Вимикати їх із коротким поясненням або вести до потрібного кроку;
      - **замінити ручні дату й час системними picker-ами:** транзакція, ціль і нагадування
        не мають вимагати введення `РРРР-ММ-ДД` чи `21:00`; новій цілі не ставити сьогоднішню
        дату як мовчазний дедлайн — запропонувати майбутню дату або лишити поле порожнім;
      - **додати наступну дію в порожні стани:** «Місяць» і «Звіти» мають вести до
        «Записати першу витрату» / «Підключити monobank», а порожній блок цілей — до
        «Створити ціль»; CTA мають бути однаковими на всіх екранах;
      - **не вести iOS-користувача в тупик «Сповіщення банків»:** позначити пункт як
        «Лише Android» із поясненням або приховати його на непідтримуваній платформі;
      - **полегшити довгі довідники:** у Категоріях і Лімітах додати пошук/фільтр, відділити
        службові значення й замінити постійні кнопки кожного рядка контекстною дією або
        свайпом, щоб список можна було швидко сканувати;
      - **підтвердити успішний запис і дати його скасувати:** після «Записати» показувати
        помітний статус над таббаром із дією Undo, а для регулярних витрат — «Повторити»;
        визначити, чи форма лишається на поточному скролі для пакетного вводу, чи повертає
        користувача до підсумку, і зробити це передбачуваним;
      - **довести accessibility:** чипи типу, рахунку й категорії мають бути кнопками з
        озвученим selected/disabled state; кожен пункт таббару — окремо доступний; перевірити
        Dynamic Type, розмір tap target і контраст дрібних сірих та золотих підписів;
      - **зробити Звіти корисними для рішення, а не лише перегляду:** додати порівняння з
        попереднім місяцем, top-категорії/мерчанти, виконання лімітів, чистий грошовий потік
        і окремий тренд загального капіталу; якщо доступна одна категорія, обирати її
        автоматично замість додаткового тапу.
- [ ] Перше число місяця робить застосунок порожнім, а «Залишилось» лякає. Побачено
      2026-09-01 на емуляторі; обидва рядки — свідомі рішення специфікації, тому це зміна,
      а не тиха правка:
      - «Місяць» відкривається на поточному місяці й каже «У цьому місяці ще нічого не
        записано», а «Звіти» показують 0,00 у всіх трьох рядах. Два екрани з трьох порожні
        кожного початку місяця. Треба вирішити, що показувати замість порожнечі:
        попередній місяць чи його підсумок окремим рядком;
      - «Залишилось −2650,00 UAH» — найбільше число екрана, від'ємне, бо дохід за місяць
        ще не записаний. Формула правильна (вимога capability `monthly-picture`), але як
        щоденний індикатор читається як катастрофа щомісяця до зарплати. Треба або інша
        назва рядка, або інша подача.
- [ ] Дрібниці, які показав ручний прохід 2026-09-01 (окремий пункт, бо жодна вимога їх
      не описує):
      - на «Перших кроках» лічильник «ГОТОВО 2 З 4» у верхньому регістрі читається як
        «2 3 4»: українська «З» на цьому шрифті з letter-spacing не відрізняється від
        цифри 3. Треба «2 із 4», «2/4» або цей рядок без uppercase;
      - розбивка однієї категорії за місяць (`/category/…`) показує транзакції, але не має
        ні підсумку категорії, ні того, на скільки перевищено ліміт — лише червона назва;
      - Головний зберігає позицію скролу між вкладками: повернувшись, потрапляєш у середину
        форми запису, а не до її початку;
      - на «Сповіщеннях банків» кнопка спершу «Додати застосунок», а після «Скасувати» —
        «Додати»; підпис має бути один;
      - список банків там захардкоджений, а не зібраний із застосунків, установлених на
        цьому телефоні;
      - на екрані monobank синхронізація лише ручна, і ніде не написано, коли вона
        відбулася востаннє.
- [ ] `investments-value` — крок 10 (0/12): ручна поточна вартість інвестиційного рахунку,
      вкладено та прибуток/збиток (FR-I1–I2).
- [ ] Дрібниці, які показав емулятор на смоуку `limits-goals-reports` (жодна вимога їх не
      покриває, тому це окремий пункт, а не тиха правка):
      - відмови у формах лімітів і цілей показують сирі англійські рядки движка в
        українському інтерфейсі: «Не збережено / an amount is positive, got "0"» і
        «Не збережено / "" is not an amount». Сама відмова правильна, локалізації немає.
        Порожня назва цілі вже сказана по-людськи («Ціль потребує назви») — решта має
        звучати так само;
      - на екрані «Ліміти» апаратна кнопка «назад» не виходить з екрана, поки відкритий
        редактор ліміту (стрілка ← у шапці працює);
      - графіки «Звітів» не мають жодних чисел чи осі — тільки смужки й підписи місяців.
        Специфікація каже «показати» числа, але формату не називає; треба вирішити, який
        він, і дописати вимогу.
- [ ] `backup-file` — крок 11: версіонований повний експорт/імпорт одного локального файла,
      перевірка цілісності й попередній перегляд відновлення; monobank-токен та інші секрети
      не експортувати (FR-B1–B2). Зміна вже в дереві (15/16), але ручний прохід
      2026-09-01 показав, що саме зберігання треба переробити до архівації:
      - «Зберегти у файл» відкриває **вибір теки** (SAF `OPEN_DOCUMENT_TREE`), стартує в
        корені сховища, і Android відповідає «Can’t use this folder — to protect your
        privacy, choose another folder», а далі просить доступ до всієї теки Documents
        заради одного файла. Перший тап власника впирається в системну помилку.
        Треба «зберегти як» з готовим іменем (`createDocument`) або share-sheet;
      - після успіху сказано «Бекап від 2026-09-01 збережено: 2 рахунки, 7 транзакцій»,
        але ні імені файла, ні шляху; статус зникає разом з екраном. Треба «останній
        бекап», який переживає перезапуск — це ж поле потрібне кроку 12.
- [ ] `google-drive-backup` — крок 12, поверх готового формату `backup-file`:
      - опціональне підключення Google-акаунта з мінімальним Drive scope; зберігати бекап у
        службовій папці застосунку в Google Drive, не просити доступу до всіх файлів;
      - автоматичний best-effort запуск раз на 24 години через Android WorkManager / Expo
        Background Task, а якщо система відклала роботу — catch-up при наступному відкритті;
      - оновлювати один актуальний бекап атомарно й тримати невелику ротацію попередніх версій;
        перед відновленням перевіряти версію схеми, checksum і показувати, яку дату буде відновлено;
      - у Налаштуваннях показувати «останній успішний бекап», «остання помилка», ручні
        «Зберегти зараз», «Відновити» і «Від'єднати Google Drive»;
      - визначити й протестувати конфлікти: у v1 це **резервна копія одного телефона**, а не
        live-sync двох пристроїв; новий телефон відновлює явно вибрану версію;
      - не класти в Drive токени, OAuth-секрети, сирі payload-и прочитаних сповіщень і локальну
        чергу capture; окремо ухвалити формат шифрування бекапу та спосіб відновлення ключа.
- [ ] `reminders-and-alerts` — крок 13: локальні системні сповіщення самого cap1tal:
      - налаштування «Нагадувати про витрати» з увімкненням/вимкненням і вибором часу; типово
        одне нагадування на день, тап відкриває Головний/чернетки для додавання й перевірки;
      - переплановувати нагадування після зміни часу, timezone або перезавантаження телефона;
      - повідомляти про помилки, які потребують дії: невдалий імпорт, незбережені дані,
        невдалий локальний/Drive-бекап, зупинений доступ до банківських сповіщень або чергу,
        яку не вдалося обробити; тап веде на екран із деталями й повтором;
      - не дублювати однакову помилку, не показувати технічні секрети/банківський текст у
        lock-screen повідомленні та не вимагати зовнішнього push-сервера; це локальні notifications.

- [ ] Звіт «Імпорт Saldo» говорить англійською там, де його читає власник. Блок «Рядки, які
      нічого не рухають» друкує причину рівно так, як її називає движок:
      `accrual-month-divergence: the row is accrued to 2024-11 and dated 2024-10-30; the import
      keeps the date`. Це не відмова форми, тож вимога app-shell «A refusal the owner reads is in
      Ukrainian» сюди не дістає — але це англійський технічний рядок посеред українського екрана,
      і власник саме на нього дивиться, щоб зрозуміти, чого бракує в імпорті. Побачено на
      емуляторі 2026-09-01 (`.cache/android/smoke/70-report.png`). Перекласти причини
      `saldo-import` у слова власника, як це вже зроблено для відмов форм.

## Питання

(порожньо — агент пише сюди, коли не може спитати інтерактивно; власник відповідає
просто під питанням і прибирає `[?]` не треба — агент зробить це сам)
