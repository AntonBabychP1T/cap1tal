# dependencies / build
node_modules/
.expo/
dist/
web-build/
coverage/
*.tsbuildinfo

# native build output (android/ itself is generated by `expo prebuild`; commit it only deliberately)
android/app/build/
android/build/
android/.gradle/
android/.idea/
android/local.properties
ios/Pods/
ios/build/

# secrets — also denied to the agent in .claude/settings.json
.env
.env.*
!.env.example
*.jks
*.keystore
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist
secrets/

# personal data — never commit exports of real finances
saldo_export*.csv
data/private/

# verify stamp and hook state
.cache/

# Claude Code local files
.claude/settings.local.json
CLAUDE.local.md

# OS / editor
.DS_Store
*.log
npm-debug.log*
